---
name: p1as:p1as-references
description: Get the P1AS reference guide — where to find information about P1AS systems.
when_to_use: |
  Use this skill to invoke the `p1as_references` operation directly.
  Trigger when the user asks specifically about p1as references or when a workflow skill
  delegates this specific operation.
---

# /p1as:p1as-references

Get the P1AS reference guide — where to find information about P1AS systems.

**MCP tool:** `mcp__server__p1as_references`

## What To Do

1. Call `mcp__server__p1as_references(topic)`.
2. Show the structured result to the user in a readable format.
3. Suggest next steps if applicable (e.g., related tools to call, follow-up actions).

