---
name: gitops:references
description: Get the gitops-mcp reference guide — where to find information about gitops-mcp systems.
when_to_use: |
  Use this skill to invoke the `references` operation directly.
  Trigger when the user asks specifically about references or when a workflow skill
  delegates this specific operation.
---

# /gitops:references

Get the gitops-mcp reference guide — where to find information about gitops-mcp systems.

**MCP tool:** `mcp__gitops__references`

## What To Do

1. Call `mcp__gitops__references(topic)`.
2. Show the structured result to the user in a readable format.
3. Suggest next steps if applicable (e.g., related tools to call, follow-up actions).

